---
import AuthLayout from "../../layouts/AuthLayout.astro";
import { defaultLocale, t } from "../../lib/i18n";
import AuthEntry from "../../components/AuthEntry";
import AuthRedirect from "../../components/AuthRedirect";
---
<AuthLayout titleKey="auth.register.title">
  <AuthRedirect client:load />
  <section class="glass w-full max-w-md rounded-lg p-6">
    <p class="subtle-label" data-i18n="auth.register.eyebrow">{t(defaultLocale, "auth.register.eyebrow")}</p>
    <h1 class="mt-3 text-3xl font-semibold tracking-tight text-white" data-i18n="auth.register.heading">{t(defaultLocale, "auth.register.heading")}</h1>
    <p class="mt-3 text-sm leading-6 text-zinc-400" data-i18n="auth.register.copy">{t(defaultLocale, "auth.register.copy")}</p>
    <AuthEntry mode="register" client:load />
    <a class="mt-5 inline-flex text-sm text-zinc-400 hover:text-white" href="/auth/login" data-i18n="auth.register.hasAccount">{t(defaultLocale, "auth.register.hasAccount")}</a>
  </section>
</AuthLayout>
