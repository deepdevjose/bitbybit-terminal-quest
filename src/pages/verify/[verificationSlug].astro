---
export const prerender = false;

import AppLayout from "../../layouts/AppLayout.astro";
import CertificateView from "../../components/CertificateView";

const { verificationSlug } = Astro.params;
---
<AppLayout titleKey="app.title">
  <CertificateView verificationSlug={verificationSlug} client:load />
</AppLayout>
